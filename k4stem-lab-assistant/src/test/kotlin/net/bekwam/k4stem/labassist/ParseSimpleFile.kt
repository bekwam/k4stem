package net.bekwam.k4stem.labassist

fun main(args : Array<String>) {

    // create a person object based on the contents of ParseSimpleFile.kt

    // first, hardcode a person string and pass that in as in the klaxon example

    // next, read in the file input and use that as the argument to klaxon
}